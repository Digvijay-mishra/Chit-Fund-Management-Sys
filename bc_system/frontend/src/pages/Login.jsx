import React,{useState} from 'react';
import { Button, Form, Input, message, Tabs } from 'antd';
import { api } from '../api/client.js';
export default function Login({onLogin}){const [loading,setLoading]=useState(false); const submit=async(v,mode)=>{try{setLoading(true); const {data}=await api.post(`/auth/${mode}`,v); onLogin(data.token)}catch(e){message.error(e.response?.data?.message||'Failed')}finally{setLoading(false)}}; return <div className="login"><div className="login-card"><h1>BC Manager</h1><p className="muted">Login or create admin account</p><Tabs items={[{key:'login',label:'Login',children:<Form layout="vertical" onFinish={v=>submit(v,'login')}><Form.Item name="email" label="Email" rules={[{required:true}]}><Input/></Form.Item><Form.Item name="password" label="Password" rules={[{required:true}]}><Input.Password/></Form.Item><Button type="primary" htmlType="submit" loading={loading} block>Login</Button></Form>},{key:'register',label:'Register',children:<Form layout="vertical" onFinish={v=>submit(v,'register')}><Form.Item name="name" label="Name" rules={[{required:true}]}><Input/></Form.Item><Form.Item name="email" label="Email" rules={[{required:true}]}><Input/></Form.Item><Form.Item name="password" label="Password" rules={[{required:true}]}><Input.Password/></Form.Item><Button type="primary" htmlType="submit" loading={loading} block>Create Account</Button></Form>}]} /></div></div>}
